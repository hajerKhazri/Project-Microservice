server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: Proxy API requests if we want to use the Nginx as a gateway point
    # but for now we'll stick to direct Gateway calls from the browser.
}
